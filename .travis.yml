language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t lyoshas/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lyoshas/docker-react npm run test
